<!DOCTYPE html>
<html>
@{
    Layout = "~/Views/Shared/_Layout.cshtml";
@model UKADPerformanceTask.Models.PerformanceModel
        }
<h2>Index</h2>
<body>    
@using (Html.BeginForm("Index", "Home", FormMethod.Post))
{
    <div>Input url value to find sitemap</div>
    @Html.TextBoxFor(model => model.Url);
    <button type="submit">TestPerformance</button>
}
@if (ViewBag.Sitemap != null)
{
    <ul>
        @foreach (var dictEntity in ViewBag.Sitemap)
        {
            <li>@dictEntity.Key - @dictEntity.Value</li>
        }
    </ul>
}
</body>
</html>